console.log('hello')

/**
 * just summs two variables
 * @param {*} a represents irst argument
 * @param {*} b represents second argument
 * @returns any
 */
const sum = (a, b) => a + b

console.log(sum(5, 7))